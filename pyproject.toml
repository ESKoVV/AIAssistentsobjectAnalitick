[project]
name = "regional-analytics-assistant"
version = "0.1.0"
description = "Monorepo skeleton for regional analytics platform"
requires-python = ">=3.11"
dependencies = [
    "fast-langdetect>=1.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.0.0",
]
